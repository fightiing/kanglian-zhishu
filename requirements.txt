# ============= Flask 核心（必须） =============
Flask==2.3.3
flask-cors==4.0.0
waitress==2.1.2

# ============= LLM / HTTP （必须）=============
requests==2.31.0

# ============= 可选依赖（没装也能跑）： =============
# pip install - requirements里声明的以下依赖是可选的。
# 如需要开启ASR语音识别+TTS+OCR，用下面的完整版requirements：
# Flask flask-cors waitress requests numpy jieba scipy soundfile opencv-python-headless Pillow rapidocr-onnxruntime baidu-aip
